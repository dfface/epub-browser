# EPUB Browser

> قراءة EPUB وPDF في مكتبة خاصة أو نشرهما كموقع ثابت مستقل.

**README:** [English](../../README.md) | [简体中文](README.zh-CN.md) | [繁體中文](README.zh-TW.md) | [日本語](README.ja.md) | [한국어](README.ko.md) | [Español](README.es.md) | [Deutsch](README.de.md) | [Français](README.fr.md) | [Русский](README.ru.md) | [Italiano](README.it.md) | [Português (Brasil)](README.pt-BR.md) | [العربية](README.ar.md) | [Bahasa Indonesia](README.id.md) | [हिन्दी](README.hi.md) | [Tiếng Việt](README.vi.md) | [ไทย](README.th.md) | [Bahasa Melayu](README.ms.md)

**لغات الواجهة (17):** الإنجليزية، والصينية المبسطة، والصينية التقليدية، واليابانية، والكورية، والإسبانية، والألمانية، والفرنسية، والروسية، والإيطالية، والبرتغالية البرازيلية، والعربية، والإندونيسية، والهندية، والفيتنامية، والتايلاندية، والملايوية.

[![PyPI version](https://img.shields.io/pypi/v/epub-browser)](https://pypi.org/project/epub-browser/)
[![Python versions](https://img.shields.io/pypi/pyversions/epub-browser)](https://pypi.org/project/epub-browser/)
[![License](https://img.shields.io/github/license/dfface/epub-browser)](../../License.txt)

![صفحة PDF داخل قارئ EPUB Browser المشترك.](../releases/assets/v2.8.0-pdf-reader.png)

يعالج EPUB Browser ملفات `.epub` و`.pdf` في وضعين بمسؤوليات منفصلة بوضوح:

| | `ssg` | `server` |
| --- | --- | --- |
| EPUB وPDF | نعم | نعم |
| النشر | استضافة ثابتة، وPages، وتخزين كائنات، وNginx | خدمة قراءة خاصة ودائمة |
| الحسابات | لا توجد | حسابات محلية |
| تسجيل الدخول الموحّد عبر OIDC | غير مضمن | Provider عام، وربط الحسابات الحالية، وإنشاء الأعضاء اختياريًا |
| التقدّم والتعليقات ورف الكتب | في هذا المتصفح فقط | بيانات الحساب المسجّل دخوله في SQLite |
| تحديث المصادر | تشغيل `ssg` مرة أخرى | إعادة تشغيل الخدمة أو استخدام `--watch` |
| قاعدة بيانات وقت التشغيل | لا توجد | مطلوبة |

يُعامل PDF كتنسيق كتاب من الدرجة الأولى: تصبح الصفحة الأولى `chapter_0.html`، وتظهر كل صفحة في الفهرس، ويعرضها PDF.js المحلي داخل المكتبة وصفحة الكتاب وواجهة القراءة والبحث ومسار التعليقات نفسها. تُخفى إمكانات PDF غير المدعومة، مثل القراءة بالذكاء الاصطناعي، بوضوح ولا يلزم أي CDN أثناء القراءة.

استخدم `ssg` عندما تريد نشر ملفات ثابتة عادية. واستخدم `server` عندما تحتاج إلى حسابات، أو بيانات مشتركة بين الأجهزة، أو التحكم في الوصول إلى الكتب، أو المراقبة التلقائية للمصادر.

## نظرة عامة

### لماذا EPUB Browser؟

- **قراءة أصلية بالذكاء الاصطناعي ترتكز إلى النص:** في وضع Server، ولملفات EPUB فقط، تبقى أدلة الفصول والشروح المدعومة بالأدلة وخرائط الأفكار وأسئلة التأمل ومحادثات Ask AI الخاصة بجوار النص الأصلي، لا في ملخص عام منفصل عنه.
- **رؤى قراءة خاصة:** في وضع Server، لا يرى وقت القراءة الفعلي وتقويم النشاط والاتجاهات والجلسات والكتب الأكثر قراءةً إلا الحساب المسجّل دخوله حالياً.

![دليل فصل بالذكاء الاصطناعي بجوار نص EPUB الأصلي، مع محادثة Ask AI خاصة.](assets/ai-native-reading.png)

*يبقى الإرشاد بالذكاء الاصطناعي والأسئلة الخاصة مرتبطين بالكتاب الأصلي.*

![رؤى قراءة خاصة تعرض تقويم النشاط واتجاه وقت القراءة.](assets/reading-insights.png)

*تحوّل رؤى القراءة وقت القراءة الفعلي إلى سجل خاص يسهل فهمه.*

### المكدس التقني

تستخدم الواجهة HTML دلالية وCSS وVanilla JavaScript بلا إطار SPA. تعتمد أداة CLI وServer على Python 3.9+ وStarlette وUvicorn وSQLite، بينما تتولى pypdf وpypdfium2 وPDF.js معالجة PDF محلياً؛ ولا يحتاج التشغيل إلى CDN.

### مواقع تجريبية

- **وضع SSG**: [epub-browser-test.yuhan.tech](https://epub-browser-test.yuhan.tech/)
- **وضع Server**: [epub.yuhan.tech](https://epub.yuhan.tech/) — اسم المستخدم وكلمة المرور: `demo`.

### القراءة الأصلية بالذكاء الاصطناعي (Server فقط)

تبني ميزة القراءة بالذكاء الاصطناعي طبقة تعليمية مشتركة وقابلة للمراجعة فوق النص الأصلي، بدلاً من وضع ملخص عام منفصل بجوار الكتاب. وتشمل مساراً قبل القراءة، ونظرة عامة اختيارية للفصل، وشروحاً مرتبطة بالاقتباسات، وملاحظات عن وظيفة الفقرات، وتوضيحات للمفردات، وشرحاً مبسطاً في النهاية، وأسئلة لمواصلة التفكير.

تُنشأ النتائج عبر مهام في الخلفية، وتُحفظ في SQLite، ويشاركها القرّاء الذين يملكون حق الوصول إلى الكتاب. وتبقى محادثات المتابعة خاصة بكل حساب. يجب على المسؤول إعداد مزوّد متوافق مع OpenAI ومنح كل عضو الإذن صراحةً. يُرسل نص EPUB المحدد إلى ذلك المزوّد، لذلك لا تفعّل الميزة إلا بموافقة القرّاء. لا تتضمن مخرجات SSG حسابات أو عناصر تحكم للذكاء الاصطناعي أو مهاماً أو إعدادات المزوّد.

## البدء

### المتطلبات والتثبيت

- Python 3.9 أو أحدث
- ملف `.epub` أو `.pdf` واحد أو أكثر، أو مجلدات متداخلة تحتوي على كتب، أو مكتبة بنمط Calibre

يدعم التثبيت من PyPI وضعي SSG وServer:

```bash
pip install epub-browser

# المساعدة الكاملة لكل وضع
epub-browser --help
epub-browser ssg --help
epub-browser server --help
```

لتشغيل Server دائم عبر Docker، استخدم الصورة المنشورة؛ ولا يحتاج المضيف إلى Python:

```bash
docker pull dfface/epub-browser:latest
```

### بدء سريع

#### إنشاء موقع ثابت

```bash
epub-browser ssg /path/to/books \
  --output-dir /path/to/dist
```

قدّم `dist/` عبر HTTP، ولا تفتح الصفحات المنشأة مباشرةً باستخدام `file://`. للنشر تحت مسار فرعي، أضف `--base-path /my-repository/`؛ يغيّر هذا الخيار عناوين URL المنشأة لا مجلد الإخراج.

#### تشغيل مكتبة Server دائمة

```bash
epub-browser server /path/to/books \
  --server-dir /path/to/epub-browser-state \
  --watch
```

افتح `http://127.0.0.1:8000/`. في الزيارة الأولى أنشئ المسؤول الأول؛ لن تُفحص المكتبة أو تُنشر قبل إكمال هذا الإعداد. يمنع `--no-browser` الخدمة من فتح المتصفح المحلي تلقائياً فقط.

## البيانات والتشغيل

### البيانات والحسابات وحدود الوصول

لكل كتاب `book_id` ثابت. يحفظ `--book-id-storage sidecar` الهوية بجوار ملف المصدر افتراضياً من دون تغيير وحداته الثنائية. يكتب `--book-id-storage embedded` الهوية في بيانات OPF لملفات EPUB، أما PDF فيعود دائماً إلى ملف sidecar المجاور.

في وضع Server، يُعد `--server-dir` الموقع الموثوق لقاعدة SQLite وذاكرات التخزين المؤقت ونسخ الترحيل الاحتياطية. كما تُحفظ فيه الحسابات ورفوف الكتب والتقدّم والتعليقات ونتائج الذكاء الاصطناعي والمهام. يدير المسؤولون المستخدمين والأدوار والجلسات وأذونات الكتب؛ ولا يستخدم الأعضاء إلا الكتب المصرح بها وبياناتهم الخاصة. احمِ أذونات هذا المجلد ونسخه الاحتياطية.

### Docker والوكيل العكسي والتوثيق الكامل

في الحاويات، اربط الكتب للقراءة فقط واربط `--server-dir` بوحدة تخزين دائمة. لا تقبل ترويسات الوكيل إلا من وكلاء موثوقين، واستخدم HTTPS في عمليات النشر العامة.

للاطلاع على Docker Compose وجميع خيارات CLI وعمليات الترحيل والشبكة المحلية والوكيل العكسي واستكشاف الأخطاء، راجع [README الكامل بالإنجليزية](../../README.md) أو [README الكامل بالصينية المبسطة](README.zh-CN.md). سلوك الوضعين واحد في جميع اللغات.

## التطوير والترخيص

### المساهمة والترخيص

نرحب بالـ Issues والـ Pull Requests. راجع [License.txt](../../License.txt) لمعرفة الترخيص.
